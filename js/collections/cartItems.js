/**
 * Created by anuraggrover on 19/06/15.
 */

( function () {
    'use strict';

    define( [
        'collections/baseCollection'
    ], function ( BaseCollection ) {
        var CartItemCollection;

        CartItemCollection = BaseCollection.extend( {

        } );

        return CartItemCollection;
    } );

} )();