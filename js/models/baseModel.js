/**
 * Created by anuraggrover on 16/06/15.
 */

( function () {
    'use strict';

    define( [
        'backbone'
    ], function ( Backbone ) {
        var BaseModel;

        BaseModel = Backbone.Model.extend( {

        } );

        return BaseModel;
    } );

} )();