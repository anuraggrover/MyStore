/**
 * Created by anuraggrover on 16/06/15.
 */

( function () {
    'use strict';

    define( [
        'models/baseModel'
    ], function ( BaseModel ) {
        var CartModel;

        CartModel = BaseModel.extend( {

        } );

        return CartModel;
    } );

} )();